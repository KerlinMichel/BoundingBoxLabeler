let config = {
    images: ['http://example.com/image.png'], // A list of image urls 

    classes: [["person", 0], ["dog", 1], ["cat", 2]] // A list of tuples with label name and label id
                                                     // Label Ids must be unqiue 
}